import React from "react";
import PropTypes from "prop-types";

function DriverLicense({ disabled = false }) {
  return <div>DriverLicense</div>;
}

DriverLicense.propTypes = { disabled: PropTypes.bool };

export default DriverLicense;
