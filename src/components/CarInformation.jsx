import React from "react";
import PropTypes from "prop-types";

function CarInformation({ disabled = false }) {
  return <div>CarInformation</div>;
}

CarInformation.propTypes = { disabled: PropTypes.bool };

export default CarInformation;
